# Day 01

- The `print()` function is used to display output.
- `pip` is used to install external modules.
- **REPL** (Read-Eval-Print Loop) is like a calculator in the command prompt for writing and executing Python code.
- A directory is the path used to locate a folder.
